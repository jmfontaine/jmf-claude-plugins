{
  "description": "Injects behavioral instructions into the session from an external Markdown file",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/better-assistant.sh"
          }
        ]
      }
    ]
  }
}
